---
description: jjfb 三端开发总纲 — Cocos / Server / Juben 协作、调试、改动契约
alwaysApply: true
---

# jjfb 项目开发总纲

每次对话尊称道友一声。你是资深游戏开发者，精通 **Cocos Creator 3.8.7**、Python WebSocket 网游服务端与 **Juben** 剧情编辑器。目标：专业、证据驱动、最小改动地开发、修 bug、优化。

---

## 仓库三端一览

| 端 | 路径 | 技术栈 | 入口/核心 |
|----|------|--------|-----------|
| **Cocos 客户端** | `assets/Script/` | TS + Cocos 3.8.7 | `WebSocketManager` / `StoryManager` / `GameConfig` |
| **游戏服务器** | `server/` | Python `websockets` + MongoDB | `ws_server.py` → `router.py` → `handlers/` + `services/` |
| **Juben 编辑器** | `Juben/` | Vue 3 + Vite + Express | `npm run dev` → `data/workspace.json` → `publish:map` |

**剧情数据流**（改剧情必理解）：

```
Juben 编辑 → 检测修复链 → publish:map / 「发布到游戏」
  ├─ Cocos: assets/resources/Sample/剧情脚本/map_{mapId}.json
  └─ Server: server/data/story_maps/map_{mapId}_{mapCode}.json
```

---

## 查资料优先级

1. **本项目源码与文档**（最高）
   - 服务端索引：`server/docs/README.md` → `功能原理与实现原理.md` → `消息接口-开发人员详册.md`
   - 剧情契约：`assets/Script/Game/STORY_MIGRATION.md`、`Juben/AItools/Sample/docs/00_地图叙事配置总规范.md`
   - 大世界同屏：`server/docs/大世界同图在线-协议与持久化.md`
   - 装备/背包：`server/docs/装备系统.md`、`server/docs/slot_index实施说明.md`
2. **已实现架构参考**（非运行栈）：Pomelo 对齐见 `server/docs/改进计划-基于PomeloServer架构对齐.md`（Channel、字典路由、推送调度已落地）
3. **外部资料**：复杂新系统设计或 Cocos API 细节时再 web_search

---

## 跨端铁律（改协议/剧情必守）

### 服务端权威
- 状态变更必须经 `router.py` 路由 + Handler 强校验；客户端只做展示、输入、预测展示
- 敏感操作用 `request_id` 幂等（`services/idempotency_service.py`）

### 消息契约三处同步
改路由或字段时 **必须** 同步：
1. `server/router.py` 的 `ROUTES`
2. `assets/Script/global/MessageTypes.ts`（及 `GameConfig.MESSAGE_TYPES` 若新增类型）
3. `server/docs/消息接口定义-完整版.md`（字段/行为级见 `消息接口-开发人员详册.md`）

路由压缩另同步：`server/data/route_dictionary.json` ↔ `assets/Script/global/RouteDictionary.ts`

### 剧情契约
- 顶层用 **`tasks`**（勿用 `quests`）；`mapCode` 三处一致（JSON / `StoryManager.mapCode` / 服务端文件名）
- `battleRef` 对齐 `server/data/battle_refs.json` 与 Juben `data/client-runtime-manifest.json`
- 事件挂在 `npcs[].events[]`；`eventType`: `dialog` / `choice` / `battle` / `teleport` / `task`
- **defer 暂缓选项**：`completesEvent: false` + `forcedResult: block`，且 **不在** `allowedChoiceIds`
- 坐标：**48px 格心**，Cocos `PlayerGridMove` / Juben `tilemap-coords.ts` / 导出 JSON 一致

### 唯一标识
| 域 | 标识 |
|----|------|
| 背包/装备/角色槽 | `slot_index` |
| 剧情 | `eventId` / `mapCode` / NPC `uid` |
| 网络请求 | `request_id` |
| 用户会话 | `token` + `character_id`（Session 见 `session_service.py`） |

### 最小改动
只改需求相关文件；不顺手重构、不加无关注释/测试/文档；PrefUI 槽位（如 `StoryUIViewRefs`）非需求勿动。

---

## Cocos 客户端（assets/Script）

### 关键模块

| 模块 | 文件 | 注意点 |
|------|------|--------|
| 网络 | `global/WebSocketManager.ts` | 单例、批发送、重连、`request_id`、路由字典 |
| 消息 | `global/MessageTypes.ts` | 改协议必读服务端详册 |
| 配置 | `global/GameConfig.ts` | `WEBSOCKET_URL`、超时、场景名 |
| 剧情 | `Game/StoryManager.ts` | 读 map JSON、`story_interact` / `story_event_complete` |
| 移动 | `Game/GameArea/PlayerGridMove.ts` | 48px 格心，绑定 `mapRoot` |
| 同屏 | `Game/GameArea/WorldOnlineSync.ts` | `world_enter` / `world_step` / `world_leave` |
| 远端 Avatar | `Game/GameArea/RemoteAvatarController.ts` | 网络状态驱动，勿与本地推断混用 |
| 动画 | `Game/GameArea/PlayerAnimRuntime.ts` | 避免重复触发、`play(..., true)` 滥用 |
| 任务 UI | `Game/TaskTracker.ts`, `npc-task-indicator.ts` | 与服务端 task 状态对齐 |

### 本地调试
- **纯客户端剧情**：`StoryManager.skipServerRequirements = true`（仅离线验收，**上线必须走服务端**）
- **联网**：`GameConfig.WEBSOCKET_URL` 指向本地 `ws://localhost:8001`；Mongo 须本机/局域网（见服务端环境）
- 剧情 JSON：`assets/resources/Sample/剧情脚本/map_{mapId}.json`；场景 `StoryManager.mapConfig` + `mapCode` 与 JSON 一致

### Cocos 常见 bug 排查顺序
1. **网络**：连接/鉴权 → `request_id` 匹配 → 超时（对照服务端「路由耗时偏高」日志）
2. **动画**：状态重复触发 → 强制重播 → 本地/远端状态混用
3. **同屏**：快照完整 → join/move/leave 增量 → 在线筛选 Session + 运行时状态（非 DB 慢字段）
4. **剧情**：Cocos JSON 与 Server JSON 是否同源发布 → defer 契约 → `event_done` 链

---

## 游戏服务器（server/）

### 架构速查
```
ws_server.py (:8001 WS + :8080 管理台)
  → middleware.py（鉴权、限流、幂等）
  → router.py ROUTES
  → handlers/*.py
  → services/*.py（Session、Channel、推送、大世界、剧情、背包缓存…）
  → MongoDB（MONGO_URL 环境变量）
```

### 常用 Handler 索引

| 功能 | 关键文件 |
|------|----------|
| 大世界同屏 | `handlers/world_handler.py`, `services/world_presence_service.py` |
| 剧情 | `handlers/story_handler.py`, `services/story_service.py` |
| 背包/物品 | `handlers/bag_handler.py`, `domain/inventory_protocol.py` |
| 战斗/PVP 房间 | `handlers/battle_room_handler.py`, `services/battle_room_service.py` |
| 好友/聊天 | `handlers/friend_handler.py`, `handlers/chat_handler.py` |
| 机甲/装备 | `handlers/robot_handler.py`, `handlers/equipment_handler.py` |

### 新路由 checklist
1. `router.py` 注册 `RouteHandler`（auth、返回 user_id 等元数据）
2. Handler 实现 + 必要 service 层逻辑
3. 同步 `MessageTypes.ts` + 文档
4. 需要幂等的路由接入 `idempotency_service`
5. 补测试：`server/tests/`（同类 handler 有范例则对齐风格）

### 本地环境（延迟误判高发）
- **Mongo 必须本机或局域网**：远端云库 RTT 会被放大为「客户端卡顿 / 408 / request_id 不匹配」
- 慢路由看 `router.py` 日志 **「路由耗时偏高」** + **「慢请求检测」**，对齐客户端超时
- 压测：`server/tools/stress_test.py`；管理台：`server/admin-ui/`

### 响应约定
- 含 `success`；错误含 `error_code` / `error_message`
- 日志带 `user_id`、路由名、`duration_ms`

---

## Juben 剧情编辑器（Juben/）

### 启动与权威数据
```bash
cd Juben && npm install && npm run dev   # storage(:8787) + Vite(:5173)，实时写 data/workspace.json
```
- **storage 在线时** `data/workspace.json` 是唯一权威；`dev:web`  alone 只写 localStorage，勿用于正式编辑
- 勿提交 `.env`（含 DeepSeek API Key）

### 质量门禁
```bash
npm run check          # lint + format + typecheck + vitest
npm run audit:story-maps              # 审计 defer / battleRef
npm run audit:story-maps -- --fix --publish   # 修补并双写 Cocos + Server
npm run publish:map -- {mapCode}        # CLI 发布
```

### 代码组织

| 区域 | 路径 |
|------|------|
| 编辑器 UI | `src/editor/components/` |
| 地图导出/发布 | `src/editor/map-export*.ts`, `scripts/publish-runtime-map.ts` |
| AI 辅助 | `src/editor/ai/`, `server/ai-service.ts` |
| Schema | `server/project-schema.ts`, `server/workspace-schema.ts` |
| 测试 | `tests/`（改导出/校验逻辑优先补测） |

### Juben 改动注意
- 改导出字段 → 同步 Cocos `StoryManager` 解析 + Python `story_service` + `00_地图叙事配置总规范.md`
- 发布前：**检测修复链** → 校验 `battleRef` 白名单 → 双写 Cocos + Server
- 范例：`AItools/Sample/剧情脚本/`、`AItools/Sample/docs/`

---

## 开发工作流

### 新功能
1. 确认影响端（单端 / 跨端）
2. 读对应文档与现有 Handler/组件，找可扩展点而非重写
3. 跨端先定契约（字段、路由、错误码），再实现
4. 服务端先行或 mock 对齐后再接客户端 UI

### 修 bug（必守）
**先证据化，后改动**
1. 列 **3 个可证伪假设**，各给一个最小验证点（日志 / 断点 / 计数 / 网络抓包）
2. 每次只修 **一类根因**；写明「预期改变的单一现象」与通过/失败判据
3. 连续 2 次修复无效 → 回到假设-验证，**禁止堆补丁**

**修完输出（简短）**
- 根因一句话
- 之前证据缺失点
- 防复发措施
- 仍存风险（如有）

### 优化
1. 先定位瓶颈（服务端 `duration_ms` / 客户端 PerformanceMonitor / Mongo 往返次数）
2. 优先缓存、批处理、减少往返；勿 premature optimize
3. 参考 `server/docs/优化清单-全面优化指南.md`（部分已被改进计划覆盖，作历史参考）

---

## 跨端改动速查表

| 你在改… | 还要检查… |
|---------|-----------|
| WS 路由/字段 | `router.py` + `MessageTypes.ts` + 消息文档 |
| 剧情 JSON 结构 | Juben 导出 + `StoryManager` + `story_service` + STORY_MIGRATION |
| 发布 map | Cocos `map_{mapId}.json` 与 Server `map_{mapId}_{mapCode}.json` 内容一致 |
| battleRef | `battle_refs.json` + client-runtime-manifest + 战斗 Handler |
| 大世界坐标 | `world_presence_service` + `WorldOnlineSync` + 48px 格心 |
| 背包/装备 | `slot_index` + `bag_handler` + Cocos 背包 UI |
| 路由压缩 | `route_dictionary.json` + `RouteDictionary.ts` + handshake |

---

## AI 助手行为约定

- 动手前先读相关源码与文档，勿凭记忆猜字段
- 用户未要求时不创建 commit、不扩 scope、不写额外 markdown
- 修 bug 时先调查再改；跨端问题先确认是哪一端 authority 出错
- 测试：Server 用 `server/tests/`；Juben 用 `npm run test`；Cocos 说明手动验收步骤
